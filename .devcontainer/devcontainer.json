{
    "name": "Zephyr Development",
    "image": "zephyrprojectrtos/zephyr-build:v0.29.0",
    "customizations": {
        "vscode": {
            "settings": {
                "terminal.integrated.defaultProfile.linux": "bash"
            }
        }
    },
    "postStartCommand": "if [ ! -d '.west' ]; then west init -l firmware; fi && west update"
}
