tests:
  cloudsense.app.smoke:
    tags: cloudsense
    platform_allow: native_sim
    harness: console
    harness_config:
      type: multi_line # all lines below must be present in the output
      regex:
        - "Hello from CloudSense!"
        - "Temperature: 25.000000 C"
    timeout: 3 # seconds
