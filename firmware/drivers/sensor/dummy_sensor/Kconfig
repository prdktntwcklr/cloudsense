config CUSTOM_SIMULATED_SENSOR
    bool "CloudSense Simulated Sensor"
    default y
    depends on DT_HAS_CLOUDSENSE_SIMULATED_SENSOR_ENABLED
    select SENSOR
    help
      Enable support for the CloudSense simulated temperature sensor.
